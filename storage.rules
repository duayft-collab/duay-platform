rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /tenants/{tid}/{allPaths=**} {
      allow read, write: if request.auth != null;
    }

    // Diğer tüm path'ler ANONIM ERİŞİME KAPALI
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
